import React from 'react';
import LiveStaffChat from '../../components/LiveStaffChat.jsx';

const LiveSupport = () => {
    return (
        <div className="h-full w-full overflow-hidden">
            <LiveStaffChat />
        </div>
    );
};

export default LiveSupport;
